{
    "lambda": {
        "lambda_handler": "{{cookiecutter.repo_name}}.lambda_handler",
        "aws_region": "us-east-1",
        "profile_name": "{{cookiecutter.lambda_role}}",
        "project_name": "{{cookiecutter.repo_name}}",
        "runtime": "python3.6",
        "s3_bucket": "zappa_{{cookiecutter.repo_name}}"
    }
}